#include<iostream>
#include<queue>
#include<utility>
#include<vector>
#include<map>
using namespace std;
int main(void){
    int kase , size , row , loc , des;
    cin >> kase;
    for(int k = 0 ; k < kase ; k++){
        cin >> size >> row;
        cin >> loc >> des;
        vector<vector<int> >map(size , vector<int>(size , 0));
        for(int i = 0 ; i < row ; i++){
            int a , b , tv , td;
            cin >> a >> b >> tv >> td;

        }

    }
    return 0;
}
